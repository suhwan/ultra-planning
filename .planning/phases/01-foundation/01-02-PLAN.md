---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/ultraplan/templates/project.md
  - .claude/skills/ultraplan/templates/roadmap.md
  - .claude/skills/ultraplan/templates/plan.md
autonomous: true

must_haves:
  truths:
    - "Template files exist for PROJECT, ROADMAP, and PLAN documents"
    - "Templates use YAML frontmatter for metadata"
    - "PLAN template uses XML tags for task structure"
  artifacts:
    - path: ".claude/skills/ultraplan/templates/project.md"
      provides: "PROJECT.md generation template"
      contains: "## What This Is"
    - path: ".claude/skills/ultraplan/templates/roadmap.md"
      provides: "ROADMAP.md generation template"
      contains: "## Phase Details"
    - path: ".claude/skills/ultraplan/templates/plan.md"
      provides: "PLAN.md generation template"
      contains: "<task type="
  key_links: []
---

<objective>
Create document templates for PROJECT.md, ROADMAP.md, and PLAN.md.

Purpose: These templates define the document structure that the Planner agent (Phase 2) will use to generate project documentation. Templates ensure consistency and include all required sections.

Output: Three template files in `.claude/skills/ultraplan/templates/`
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-RESEARCH.md

Key patterns from research:
- YAML frontmatter for machine-readable metadata
- XML tags for task structure in PLAN.md
- Templates are prompts - they guide document generation
- Follow GSD's proven document hierarchy patterns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create templates directory and PROJECT.md template</name>
  <files>
    .claude/skills/ultraplan/templates/
    .claude/skills/ultraplan/templates/project.md
  </files>
  <action>
    Create `.claude/skills/ultraplan/templates/` directory if it does not exist.

    Create `.claude/skills/ultraplan/templates/project.md` with this content:

    ```markdown
    # {project_name}

    ## What This Is

    {one_paragraph_description}

    ## Core Value

    **{core_value_statement}**

    ## Requirements

    ### Active

    - [ ] **REQ-01**: {requirement_description}

    ### Out of Scope

    - {excluded_feature} - {reason}

    ## Context

    {technical_context}

    ## Constraints

    - {constraint_1}

    ## Key Decisions

    | Decision | Rationale | Outcome |
    |----------|-----------|---------|
    | {decision} | {why} | Pending |

    ---
    *Last updated: {date}*
    ```

    Use placeholders in {curly_braces} for dynamic content.
    This template matches the structure used in the current PROJECT.md.
  </action>
  <verify>cat .claude/skills/ultraplan/templates/project.md</verify>
  <done>project.md template exists with all required sections: What This Is, Core Value, Requirements, Context, Constraints, Key Decisions</done>
</task>

<task type="auto">
  <name>Task 2: Create ROADMAP.md and PLAN.md templates</name>
  <files>
    .claude/skills/ultraplan/templates/roadmap.md
    .claude/skills/ultraplan/templates/plan.md
  </files>
  <action>
    Create `.claude/skills/ultraplan/templates/roadmap.md`:

    ```markdown
    # Roadmap: {project_name}

    ## Overview

    {2_3_sentence_journey_summary}

    ## Phases

    - [ ] **Phase 1: {phase_name}** - {one_line_description}

    ## Phase Details

    ### Phase 1: {phase_name}
    **Goal**: {outcome_not_tasks}
    **Depends on**: {dependencies_or_nothing}
    **Requirements**: {req_ids}
    **Success Criteria** (what must be TRUE):
      1. {observable_truth}
    **Plans**: TBD (estimated N plans)

    Plans:
    - [ ] 01-01: {plan_description}

    ## Progress

    | Phase | Plans Complete | Status | Completed |
    |-------|----------------|--------|-----------|
    | 1. {name} | 0/N | Not started | - |

    ---
    *Roadmap created: {date}*
    ```

    Create `.claude/skills/ultraplan/templates/plan.md`:

    ```markdown
    ---
    phase: {phase_id}
    plan: {plan_number}
    type: execute
    wave: {wave_number}
    depends_on: []
    files_modified: []
    autonomous: true

    must_haves:
      truths: []
      artifacts: []
      key_links: []
    ---

    <objective>
    {what_this_plan_accomplishes}

    Purpose: {why_this_matters}
    Output: {artifacts_created}
    </objective>

    <context>
    @PROJECT.md
    @ROADMAP.md
    @STATE.md
    </context>

    <tasks>

    <task type="auto">
      <name>Task 1: {action_oriented_name}</name>
      <files>{file_paths}</files>
      <action>{specific_implementation}</action>
      <verify>{verification_command}</verify>
      <done>{acceptance_criteria}</done>
    </task>

    </tasks>

    <verification>
    {overall_checks}
    </verification>

    <success_criteria>
    - [ ] {measurable_criterion}
    </success_criteria>

    <output>
    After completion, create SUMMARY.md
    </output>
    ```

    The PLAN.md template uses:
    - YAML frontmatter for machine-readable metadata (phase, wave, dependencies)
    - XML tags for task structure (<tasks>, <task>, <action>, <verify>, <done>)
    - must_haves section for goal-backward verification
  </action>
  <verify>cat .claude/skills/ultraplan/templates/roadmap.md && echo "---" && cat .claude/skills/ultraplan/templates/plan.md</verify>
  <done>Both templates exist with correct structure: roadmap.md has phases/progress sections, plan.md has YAML frontmatter and XML task structure</done>
</task>

</tasks>

<verification>
1. Run: `ls -la .claude/skills/ultraplan/templates/` - should show 3 files
2. Check project.md contains: "## What This Is", "## Core Value", "## Requirements"
3. Check roadmap.md contains: "## Phase Details", "**Success Criteria**"
4. Check plan.md contains: YAML frontmatter (---), `<task type=`, `<action>`, `<verify>`
</verification>

<success_criteria>
- [ ] `.claude/skills/ultraplan/templates/` directory exists
- [ ] `project.md` template has all required sections
- [ ] `roadmap.md` template has phase structure with success criteria
- [ ] `plan.md` template has YAML frontmatter AND XML task tags
- [ ] All templates use {placeholder} syntax for dynamic content
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
